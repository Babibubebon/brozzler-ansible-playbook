server {
    listen 80;
    charset utf-8;

    location /static {
        alias {{ pywb_root }}/static;
    }

    location / {
        include uwsgi_params;
        uwsgi_param SCRIPT_NAME "";
        uwsgi_pass unix:{{ pywb_root }}/pywb_uwsgi.sock;
    }
}